{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lua",
      "request": "launch",
      "name": "Debug Lua",
      "stopOnEntry": false,
      "program": "${file}",
      "cwd": "${fileDirname}"
    }
  ]
}
